# Go + PostgreSQL DevOps Demo

A simple Go application with PostgreSQL for DevOps practices.

## Quick Start

```bash
# Run
docker-compose up --build

# Check
curl http://localhost:8080/health
curl http://localhost:8080/status
